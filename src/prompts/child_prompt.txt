You are generating SEO + AEO content for an Indian real-estate Area Converter child page.

You MUST output ONLY valid JSON (no markdown, no backticks, no commentary).
All HTML must be valid, clean, and safe (no scripts, no inline styles). Use <p>, <ul>, <li>, <strong>, <em>, <table>, <thead>, <tbody>, <tr>, <th>, <td>.

CRITICAL RULES
1) Directionality: This page is ONLY about converting FROM {{from_unit_label}} ({{from_unit_code}}) TO {{to_unit_label}} ({{to_unit_code}}).
   - Content must read as “from → to”.
   - Do not write generic copy that would work equally well for the reverse page.
   - Avoid mirrored phrasing. Use distinct angles, examples, contexts, and FAQs for this direction.

2) Uniqueness vs reverse page:
   - Assume the reverse page exists ({{to_unit_label}} → {{from_unit_label}}).
   - Do NOT reuse sentences/paragraph structures that could be swapped.
   - Use distinct “why convert” motivations, distinct real-world examples, and distinct FAQs.

3) Audience: Must be understandable to lay users and Google crawlers.
   - No heavy jargon. Explain terms simply.
   - Must still sound professional and real-estate relevant.

4) India-first context:
   - Prefer Pan-India phrasing by default.
   - If a unit is clearly regional (based on the unit name itself), mention likely Indian regions/states where it appears.
   - Do NOT force city-specific phrasing for global units.
   - City context is: {{city_name}}. Use it only when it genuinely makes sense.

5) Factor handling:
   - If {{factor_to_unit}} is numeric, use it consistently in examples, the “1 from equals” line, and quick reference table.
   - If not numeric or missing, explain factor variability (regional definitions) and rely on the calculator.

6) Symbols:
   - Use the unit symbols provided by input.
   - fromUnitSymbol: {{from_unit_symbol}}
   - toUnitSymbol: {{to_unit_symbol}}

STRICT LENGTH TARGETS (aim MID-RANGE to avoid failures)
- hero.subtitle: TARGET 52–56 words (plain text, not HTML)
- whyConvert.contentHtml: TARGET 235–255 words (HTML)
- fromUnitCard.descriptionHtml: TARGET 225–255 words (HTML)
- toUnitCard.descriptionHtml: TARGET 225–255 words (HTML)
- technicalBackground.technicalExplanationHtml: TARGET 170–185 words (HTML)
- technicalBackground.precisionNotesHtml: TARGET 55–70 words (HTML)
- Each FAQ answerHtml: TARGET 75–95 words (HTML)
- quickConversionReference.rows: exactly 8 rows
- realWorldExamples.cards: exactly 3 cards
If you are short, add one extra sentence or a short <ul> to reach the target. Do NOT be concise.

OUTPUT JSON SCHEMA (must match exactly)

{
  "seo": {
    "metaTitle": "string",
    "metaDescription": "string",
    "h1Heading": "string"
  },
  "hero": {
    "slug": "string (kebab-case, e.g., sq-m-to-sq-ft)",
    "h1": "string",
    "subtitle": "string (plain text, target 52–56 words)",
    "fromUnit": { "code": "string", "name": "string", "symbol": "string", "region": "string" },
    "toUnit":   { "code": "string", "name": "string", "symbol": "string", "region": "string" },
    "oneFromEquals": "string (e.g., '1 m² = 10.7639 ft²')",
    "cityContext": "string (short; can be 'Pan-India' if city is not relevant)"
  },
  "whyConvert": {
    "heading": "string",
    "contentHtml": "string (HTML, target 235–255 words)",
    "commonUseCases": ["string", "string", "string", "string"],
    "conversionCard": {
      "left": "string (e.g., '1 m²')",
      "right": "string (e.g., '10.7639 ft²')"
    }
  },
  "majorUnitsExplained": {
    "fromUnitCard": {
      "title": "string",
      "descriptionHtml": "string (HTML, target 225–255 words; include India usage + history/origin + states + domains)",
      "whereUsedBullets": ["string", "string", "string", "string"],
      "quickEquivalence": "string (e.g., 'Quick equivalence: 1 m² = 10.7639 ft²')"
    },
    "toUnitCard": {
      "title": "string",
      "descriptionHtml": "string (HTML, target 225–255 words; include India usage + history/origin + states + domains)",
      "whereUsedBullets": ["string", "string", "string", "string"],
      "quickEquivalence": "string"
    }
  },
  "realWorldExamples": {
    "heading": "string",
    "cards": [
      { "title": "Small Apartment", "fromValue": "string", "toValue": "string", "note": "string" },
      { "title": "Family Home",     "fromValue": "string", "toValue": "string", "note": "string" },
      { "title": "Big Apartment",   "fromValue": "string", "toValue": "string", "note": "string" }
    ]
  },
  "technicalBackground": {
    "heading": "string",
    "technicalExplanationHtml": "string (HTML, target 170–185 words; plain language)",
    "differencesBullets": ["string", "string", "string"],
    "precisionNotesHtml": "string (HTML, target 55–70 words)"
  },
  "quickConversionReference": {
    "heading": "string",
    "columns": ["string", "string", "string"],
    "rows": [
      { "from": "number", "to": "number", "commonUse": "string" }
    ]
  },
  "faqs": [
    { "question": "string", "answerHtml": "string (HTML)" }
  ]
}

CONTENT GUIDANCE (must follow)
- H1 must clearly say “{{from_unit_label}} to {{to_unit_label}} Converter” or “Convert {{from_unit_label}} to {{to_unit_label}}”.
- SEO metaTitle/metaDescription must be India + real-estate oriented and directional.
- WhyConvert must reference India property contexts: listings, carpet/built-up area, floor plans/approvals, valuation, negotiations, lender docs, registry/agreements, etc.
- MajorUnitsExplained cards must include:
  - what it is
  - how it’s used in Indian real estate
  - a short origin/history note
  - where it’s seen (states/regions) if relevant
  - 4 bullets “Where is it used?”
- RealWorldExamples:
  - Values should feel realistic for Indian apartment sizes
  - Must be directional and computed using the factor
- QuickConversionReference:
  - Use 8 rows. Choose from-values intelligently and compute to-values using factor.

Now generate the JSON for this page using:
FROM: {{from_unit_label}} ({{from_unit_code}}), symbol {{from_unit_symbol}}, region {{from_unit_region}}
TO:   {{to_unit_label}} ({{to_unit_code}}), symbol {{to_unit_symbol}}, region {{to_unit_region}}
FACTOR: {{factor_to_unit}}
CITY: {{city_name}}

{{direction_note}}